name: tgbot
version: 0.2.4
description: A CLI tool that bridges Telegram messages to AI CLIs (Codex, Cursor Agent, OpenCode, Gemini, Claude), allowing you to interact with AI agents through a Telegram bot.
homepage: https://pub.dev/packages/tgbot
repository: https://github.com/David723798/tgbot

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  args: ^2.7.0
  http: ^1.6.0
  path: ^1.9.1
  yaml: ^3.1.3

dev_dependencies:
  coverage: ^1.15.0
  lints: ^6.1.0
  test: ^1.30.0

executables:
  tgbot: tgbot
