name: tgbot
version: 0.1.5
description: A CLI tool that bridges Telegram messages to AI CLIs (Codex, OpenCode, Gemini, Claude), allowing you to interact with AI agents through a Telegram bot.
homepage: https://pub.dev/packages/tgbot
repository: https://github.com/David723798/tgbot

environment:
  sdk: '>=2.15.0 <4.0.0'

dependencies:
  args: ^2.7.0
  http: ^1.6.0
  path: ^1.9.1
  yaml: ^3.1.3

dev_dependencies:
  lints: ^6.1.0
  test: ^1.30.0

executables:
  tgbot: tgbot
